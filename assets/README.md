# The outro-song slot

Drop `epilogue.mp3` in this folder and the win-screen Dyson swarm assembles
to it instead of the generative swell. Ship the itch.io build without it;
fill the slot with the week's Suno outro for the capture.
